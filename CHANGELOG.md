# Changelog

## v0.9.3 — Calendar month axis alignment

### Annual time-axis correction

- ปรับแท่งที่ 5 จากการแบ่งแบบ “วันเกิดถึงวันเดียวกันของเดือนถัดไป” เป็น **เดือนปฏิทินจริง**
- รอยต่อของเดือนใช้เวลา `00:00 น.` ของวันที่ 1 ของแต่ละเดือนตามปฏิทินจริง
- แท่งที่ 4 (อนุทักษา) และแท่งที่ 5 (เดือนปฏิทิน) ใช้ `yearStartEpochMs / yearEndEpochMs` เดียวกัน จึงทำให้ระดับแนวนอนเดียวกันหมายถึงวันเวลาเดียวกัน
- เดือนต้นปีชีวิตและเดือนปลายปีชีวิตอาจเป็นช่วงบางส่วน (partial month) เพื่อรักษาแกนเวลาให้ตรงกับวันเกิดครบรอบจริง
- เปลี่ยนหัวแท่งที่ 5 เป็น `เดือนปฏิทิน` พร้อมคำอธิบาย `แกนเวลาเดียวกัน`

### Validation

- เพิ่ม regression test ตรวจว่าเดือนปฏิทินเริ่ม/จบตรงกับปีชีวิต
- ตรวจว่ารอยต่อเดือนเกิดที่วันที่ 1 เวลา 00:00 และทุกช่วงต่อกันโดยไม่มีช่องว่างหรือช่วงซ้อน

### Versioning

- เปลี่ยนเวอร์ชันเป็น `v0.9.3`
- เปลี่ยน Service Worker cache เป็น `maha-thasa-v0.9.3`

## v0.9.2 — Annual Page polish + Thai Buddhist date input

### UI / annual page

- เปลี่ยนข้อความตัวเลือกฐานอายุจาก `อายุย่าง (ตามต้นฉบับ)` เป็น `อายุย่าง`
- ทำให้เลขอายุในแท่ง `ดาวเสวยอายุหลัก` เปลี่ยนตามโหมด `อายุย่าง / อายุเต็ม` และแสดงชื่อโหมดอายุภายในแท่งเสมอ
- หากแท่ง `ภูมิทักษา` เป็นดาว 8 จะไม่แสดงคำว่า `นอกมหาภูติ` ภายในแท่ง
- ปรับแท่ง `อนุทักษา` ให้เอาเส้นประของวันรอยต่อออก และวางข้อความวันชิดขอบรอยต่อของแต่ละช่วง
- ปรับความกว้างของแท่ง 1–4 ให้เท่ากัน
- เพิ่มแท่งที่ 5 ทางขวาของอนุทักษา เป็นแถบ 12 เดือนตามเวลาปกติ พร้อมชื่อเดือนและปี พ.ศ.

### Input form

- เปลี่ยนช่อง `วันที่ต้องการคำนวณอายุ` จาก input แบบ ค.ศ. เป็นฟอร์มแยก `วันที่ / เดือน / ปี พ.ศ.`
- ปุ่ม `ใช้วันที่วันนี้` เติมค่าลงช่อง พ.ศ. ใหม่โดยตรง

### Versioning

- เปลี่ยนเวอร์ชันเป็น `v0.9.2`
- เปลี่ยน Service Worker cache เป็น `maha-thasa-v0.9.2`

## v0.9.1 — ปรับหน้าตาแผนภาพผลประจำปี

### Annual UI polish

- ปรับเส้นประความสัมพันธ์ระหว่างแท่งดาวเสวยหลักกับแท่งดาวเสวยแทรกให้เป็นแนวนอน โดยยึดจุดกึ่งกลางของช่วงดาวแทรก
- เอาสัญลักษณ์เครื่องหมายถูก / เครื่องหมายตกใจออกจากแผนภาพ annual view เพื่อให้ภาพสะอาดขึ้น
- ปรับแท่ง **อนุทักษา** ให้เลขดาวแต่ละช่วงแสดงเพียงตำแหน่งกึ่งกลางของช่วงนั้น ไม่ซ้ำในแต่ละชิ้นย่อย
- ใช้พื้นที่ชิดซ้ายในแท่งอนุทักษาแสดงชื่อ **กาลโยคมหาภูติประจำปี** ของแต่ละชิ้นย่อยแทน
- ตัดข้อความ `นอกมหาภูติ` ออกจากแท่งอนุทักษา และให้ราหู (8) แสดงเลขสีแดงเข้มบนพื้นสีเทา
- ย้ายวันที่ของอนุทักษาไปแสดงที่เส้นรอยต่อของแต่ละช่วง เพื่อลดข้อความซ้ำซ้อน
- ตัดข้อความ `มหาภูติกำเนิด` ออกจากภายในแท่งดาวเสวยอายุหลัก และเพิ่มเลขอายุของผู้ใช้ไว้ด้านล่างภายในแท่งเดียวกัน

### PWA / versioning

- เปลี่ยนเวอร์ชันเป็น `v0.9.1`
- เปลี่ยน Service Worker cache เป็น `maha-thasa-v0.9.1`

## v0.9.0 — ภูมิทักษา + อนุทักษา + หน้าผลประจำปี

### ฟังก์ชันใหม่

- เพิ่มหน้าผลลัพธ์ลำดับที่ 3 **“ผลประจำปี”** โดยสลับจากหน้ามหาทศาได้ด้วย Tab
- เพิ่ม Engine **ภูมิทักษา (MAHA-TAKSA-07)** ใช้ลำดับดาว `[1, 2, 3, 4, 7, 5, 8, 6]` ดาวละ 1 ปี เริ่มจากดาวประจำวันเกิด
- ใช้ **อายุย่าง** เป็นค่าเริ่มต้นตามต้นฉบับ และเปิดให้เลือก “อายุเต็ม” เป็นโหมดแยกอย่างชัดเจน
- เพิ่ม Engine **อนุทักษาพยากรณ์ (MAHA-TAKSA-08)** แบ่งปีภูมิทักษาเป็น 8 ช่วง
- ใช้กำลังจำเพาะคงที่ตามตำรา `1=20, 2=50, 3=26, 4=57, 7=33, 5=64, 8=40, 6=70` รวม 360 วัน
- เพื่อแสดงวันจริงครบทั้งปี ระบบใช้โหมด `gregorian_proportional_projection` ฉายสัดส่วน 360 วันลงบนช่วงวันเกิดครบรอบจริง 1 ปี โดยติดป้ายว่าเป็นส่วนขยายสมัยใหม่

### หน้าผลประจำปี

- คอลัมน์ 1: ดาวเสวยอายุหลัก + ตำแหน่ง **กาลโยคมหาภูติกำเนิด**
- คอลัมน์ 2: ดาวเสวยแทรกที่เกิดขึ้นภายในปีอายุนั้น แบ่งความสูงตามสัดส่วนเวลาจริง + **กาลโยคมหาภูติประจำปี**
- เชื่อมคอลัมน์ 1→2 ด้วยเส้นประและวงกลมความสัมพันธ์ดาว
- ความสัมพันธ์ `supportive` ใช้เขียว, `conflicting` ใช้แดง, กรณีมีทั้งสอง polarity แสดงวงกลมครึ่งเขียว/ครึ่งแดง, คู่ที่ไม่มีข้อมูลแสดงสีเทาโดยไม่เดาผล
- คอลัมน์ 3: ดาวภูมิทักษาประจำปี + ตำแหน่งทักษา (บริวาร–กาลี) + กาลโยคมหาภูติประจำปี
- คอลัมน์ 4: อนุทักษา 8 ช่วง พร้อมวันเริ่ม–วันสิ้นสุดและสีตามกาลโยคมหาภูติประจำปี
- ราหู (8) คงสีเทา/นอกมหาภูติ เพราะกาลโยคมหาภูติใช้ดาว 1–7
- แสดงเส้นรอยต่อ **จุลศักราช/เถลิงศก** ภายในปี และเปลี่ยนสีสถานะกาลโยคทันทีโดยไม่เปลี่ยนดาวภูมิหรือดาวอนุทักษากลางช่วง
- ปุ่มบันทึกภาพรองรับหน้าผลประจำปีด้วย

### Data / Knowledge / PWA

- เพิ่ม `data/annual-forecast.json`
- เพิ่ม `js/core/annualForecast.js`
- เพิ่ม `js/components/annual-view.js`
- เพิ่ม `css/annual.css`
- เพิ่มองค์ความรู้ `knowledge/07-มหาทักษา-ภูมิทักษา.md` และ `knowledge/08-มหาทักษา-อนุทักษาพยากรณ์.md`
- เปลี่ยน PWA cache เป็น `maha-thasa-v0.9.0`
- เพิ่ม regression tests สำหรับภูมิทักษา อนุทักษา การปิดช่วงครบปี รอยต่อเถลิงศก ราหู และความสัมพันธ์แบบ mixed

## v0.8.2 — UI refinement, compact relationship labels และ Wheel export detail

### UI

- ตัดข้อความ `โหราศาสตร์ไทย · มหาทักษา` และ subtitle `ดาวเสวยอายุ 108 ปี · กาลโยคมหาภูติกำเนิด · ความสัมพันธ์ของดาว` ออกจาก header
- ตัดข้อความเตือนกรณีวันสุริยคติกับวันเกิดที่ผู้ใช้เลือกไม่ตรงกันออกจาก UI โดยยังคงยึดวันเกิดที่ผู้ใช้เลือกเป็นดาวเริ่มต้นตามเดิม
- จัดปุ่ม `แก้ไขข้อมูล`, `เริ่มใหม่`, `บันทึกภาพ` ให้อยู่ในแถวเดียวกันเสมอ รวมถึงหน้าจอมือถือ
- ตัดกล่องหมายเหตุหลักการคำนวณ จ.ศ. ด้านล่างออกจากหน้า Dashboard

### Wheel

- เอาวงกลมเส้นบางที่ล้อมเลขดาววันเกิดด้านในสุดออก
- เปลี่ยนคำ `ดาววันเกิด` เป็นชื่อพระเคราะห์จริงของวันเกิด เช่น `พระอาทิตย์`, `พระเสาร์`
- ทำ label ความสัมพันธ์บนเส้นประให้สั้นและไม่มีกรอบพื้นหลัง เพื่อไม่ชนวงกลาง
- ใช้คำสั้น: `มิตร`, `ศัตรู`, `ธาตุ`, `สมพล`
- ใช้สีแยกความหมาย: มิตรเขียว, ศัตรูแดง, ธาตุบวกน้ำเงิน, สมพลบวกฟ้า, ธาตุ/สมพลฝ่ายขัดแย้งแดง

### Export

- เมื่อบันทึกภาพในโหมดวงกลม ระบบบันทึกทั้ง Wheel และกล่อง `รายละเอียดดาวแทรก` ที่อยู่ด้านล่าง
- Timeline export คงพฤติกรรมเดิม

### PWA

- เปลี่ยน cache เป็น `maha-thasa-v0.8.2`

## v0.8.1 — Wheel render, Sarabun และ Suriyayatra CS engine

- แก้ Wheel/Timeline self-relation regression
- ใช้ Sarabun แบบ self-hosted ทั้งระบบ
- คำนวณเถลิงศกจากเกณฑ์สุริยยาตร์ภายใน WebApp และใช้ MyHora เป็น validation source

## v0.8.0 — Maha Thasa foundation

- เปลี่ยนชื่อระบบเป็นมหาทศา
- เพิ่มกาลโยคมหาภูติกำเนิดและความสัมพันธ์ดาว 6 ประเภท
