# วิธีอัปเดต มหาทศา v0.9.3 → v0.9.4

## จุดแก้หลัก

- แท่งที่ 5 แสดงเดือนเป็นรูปแบบสั้น เช่น `พ.ค.69`
- ไฮไลต์เดือนที่ตรงกับเวลาปัจจุบันจริงด้วยสีเทาเข้มขึ้น
- สัดส่วนเวลาและรอยต่อเดือนยังคงตรงกับแกนเวลาอนุทักษาเดิม
- อัปเดต PWA cache เป็น `maha-thasa-v0.9.4`

## วิธีอัปเดต

แตกไฟล์ update-only แล้วอัปโหลดไฟล์ทั้งหมดทับไฟล์เดิมใน Repository จากนั้นรอ GitHub Pages deploy ใหม่และทำ hard refresh หนึ่งครั้ง

## ทดสอบ

```bash
npm test
```

ผลการทดสอบของชุดนี้: **ผ่านทั้งหมด**
